# Case inventory

Place real candidate material under `cases/raw/`. That directory is treated as immutable. Run `node scripts/inventory-cases.js` to create a read-only filename and selected text metadata inventory. Review all findings manually. The scanner does not execute files, use OCR, sanitize assets, or claim a case is publishable.
